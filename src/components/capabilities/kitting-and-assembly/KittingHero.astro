---
import { en } from '@/i18n/locales/en';
import HeroImage from '@/components/ui/HeroImage.astro';
import { getLQIP } from '@/lib/lqip';
import heroImg from '@/assets/images/kitting-and-assembly/kitting-hero.jpg';

const placeholder = await getLQIP('src/assets/images/kitting-and-assembly/kitting-hero.jpg');
---

<section
  class="relative min-h-screen flex items-center overflow-hidden"
  aria-label="Kitting and Assembly Services"
>
  <HeroImage src={heroImg} {placeholder} alt="" />
  <div class="absolute inset-0 bg-gradient-to-r from-on-surface/85 via-on-surface/65 to-on-surface/20 z-10"></div>

  <div class="container mx-auto px-8 lg:px-16 relative z-20 py-24">
    <div class="max-w-2xl space-y-8">

      <p class="font-label text-sm uppercase tracking-widest text-secondary" data-i18n="kitting.hero.eyebrow">
        {en.kitting.hero.eyebrow}
      </p>

      <h1
        class="font-headline text-5xl lg:text-6xl xl:text-7xl font-extrabold tracking-tighter text-white leading-tight"
        data-i18n="kitting.hero.heading"
      >{en.kitting.hero.heading}</h1>

      <p
        class="text-xl text-white/90 font-light leading-relaxed border-l-4 border-secondary pl-8 max-w-xl"
        data-i18n="kitting.hero.description"
      >{en.kitting.hero.description}</p>

      <div class="pt-2">
        <a
          href="#bom-vs-kit"
          id="kitting-hero-cta"
          class="inline-block bg-gradient-to-r from-secondary to-secondary/80 text-white px-8 py-4 rounded font-label text-sm font-bold uppercase tracking-widest btn-press"
          data-i18n="kitting.hero.cta"
        >
          {en.kitting.hero.cta}
        </a>
      </div>

    </div>
  </div>
</section>

<script>
  document.getElementById('kitting-hero-cta')?.addEventListener('click', (e) => {
    e.preventDefault();
    document.getElementById('bom-vs-kit')?.scrollIntoView({ behavior: 'smooth' });
  });
</script>
