---
import { en } from '@/i18n/locales/en';
---

<section
  class="relative min-h-screen flex items-center overflow-hidden"
  aria-label="Reverse Engineering"
>
  <!-- Background image -->
  <div class="absolute inset-0 z-0">
    <img
      src="/images/reverse-engineering-hero.jpg"
      alt=""
      aria-hidden="true"
      class="w-full h-full object-cover"
      loading="eager"
    />
  </div>
  <!-- Dark gradient overlay -->
  <div class="absolute inset-0 bg-gradient-to-r from-on-surface/85 via-on-surface/65 to-on-surface/20 z-10"></div>

  <div class="container mx-auto px-8 lg:px-16 relative z-20 py-24">
    <div class="max-w-2xl space-y-8">

      <!-- Eyebrow -->
      <p class="font-label text-sm uppercase tracking-widest text-secondary" data-i18n="reverseEngineering.hero.eyebrow">
        {en.reverseEngineering.hero.eyebrow}
      </p>

      <!-- Heading -->
      <h1
        class="font-headline text-5xl lg:text-7xl font-extrabold tracking-tighter text-white leading-tight"
        data-i18n="reverseEngineering.hero.heading"
      >{en.reverseEngineering.hero.heading}</h1>

      <!-- Description with left-border accent -->
      <p
        class="text-xl text-white/90 font-light leading-relaxed border-l-4 border-secondary pl-8 max-w-xl"
        data-i18n="reverseEngineering.hero.description"
      >{en.reverseEngineering.hero.description}</p>

      <!-- CTA -->
      <div class="pt-2">
        <a
          href="#capabilities"
          id="re-hero-cta"
          class="inline-block bg-gradient-to-r from-secondary to-secondary/80 text-white px-8 py-4 rounded font-label text-sm font-bold uppercase tracking-widest hover:shadow-ambient-lg active:scale-95 transition-all duration-200 shadow-lg"
          data-i18n="reverseEngineering.hero.cta"
        >
          {en.reverseEngineering.hero.cta}
        </a>
      </div>

    </div>
  </div>
</section>

<script>
  document.getElementById('re-hero-cta')?.addEventListener('click', (e) => {
    e.preventDefault();
    document.getElementById('capabilities')?.scrollIntoView({ behavior: 'smooth' });
  });
</script>
